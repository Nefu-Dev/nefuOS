1. 图形底层：
LVGL（C++ 也可用），这些技术加上 TTF 文字库（stb_truetype（MIT）），保持兼容。

2. 网页解析：
使用 lithtml + QuickJS / Duktape + 自研 TCP，以及图片渲染、表单、Cookie、历史记录、URL 解析、跳转逻辑、字体（独立应用）。

3. 图片：
使用 https://github.com/nothings/stb 的图片库。

4. 文件和进程管理器：
底层用 C++ 管理和释放内存，主层还是第 1 项的那个。

5. 功能与代码参考：
(1) 添加开始菜单、一些常用功能和初始目录。
(2)** 参考（抄上）一些 star 多的 C/C++ 仓库代码：
(a) 参考/移植，遵守许可证；
(b) 记录仓库、版本、commit、许可证；
(c) 避免 GPL 污染；
(d) 生成第三方依赖清单 SBOM；
(e) README 标明出处、修改说明、许可证文本。
(f) FreeBSD / NetBSD / Haiku /SerenityOS（BSD/MIT 系）设为首选
**
(3) 添加多系统（确保 UEFI 引导能正常搞）：
(a) nefuOS 自带 UEFI/BIOS 引导；
(b) 检测已有 Windows 引导项，加入启动菜单；
(c) 从 nefuOS 菜单可链式加载 Windows bootloader（nefu 也能启动 Win，能互通）；
(d) 不修改 Windows 注册表和系统文件。
(f) 使用 esp 进行.通过 grub 引导启动。
(4) 关机关闭所有进程，确保正常开关机。
(5) 支持所有常用的机型、网卡等配置（特别多代码的话直接参考 FreeBSD / NetBSD / Haiku / SerenityOS（BSD/MIT 系））。
(6) 添加自恢复功能，恢复命令行（删除系统文件后可恢复）和更详细的蓝屏指令。
(7) 添加 .nefud 解析器功能和 nefupack。
(8) 数据库与管理员设置：利用你的部署系统改出来一个数据库，并创建一个管理员数据库。构建完 iso 后，将数据库地址、用户、代码 str 全改为 you-sql 的字符串，创建一个链接用于读取，让用户可以看到，并且用户无需注册（如果 admin 创建账户，就只能用游客访问，不污染 admin）。管理员用户名为 lbinm。管理员密码：admin((Argon2/bcrypt 哈希，不存明文。)登录时把输入密码哈希后与存储值比对。密码不出现在源码、README、ISO、日志里。
(9) 修改.bin 启动方法，以免 gui 启动错误。
(10) 创建一个编辑器，用于打开文件。
(11) 增加“蜜罐”防护机制和其他防护手段（确保蜜罐不违法法规和国家的《网络安全法》政策）。
(12) 添加 音频，视频文件支持和支持 NTFS/FAT32 硬盘（主流）。
(13) 将硬盘改为自创 nvfs(nefu virtual file system ),有日志更能，只拿出来用户可移动的文件，系统文件直接焊在内核里，没有隐藏文件。
(14) 将 iso 改为裸机可直接运行的 iso.而不是专门去读 nefuos.img。
(14) 为其添加开机启动项，硬件识别，真实保存、关机和内存大小，添加识别硬盘和 MSP 硬件管理和更好的 bios，和添加开机页面和待机(俗称 win 的锁屏)壁纸，登录需填 UEFL 引导设的密码(名字也在 EUFI 设)，添加真实开关机重启，为设置添加更多选项和功能。

验收：
(1) 启动时间、内存上限、72h 稳定、OOM/断网/断电恢复、测试集通过率。
(2) 冷启动 < 3 秒；
(3) 峰值 RAM < 1 GB；
(4) 连续运行 72 小时无崩溃、无泄漏；
(5) OOM 后 5 秒内进入恢复模式；
(6) HTML/JS 测试集通过率 > 80%；
(7) 所有功能都能在任何常见配置电脑、任何配置网卡上运行平稳（要是搞不好的话，以测试列表通过就可以了）。
(8)经强度 qemu 测试，不要有任何 bug 和渲染错误。

禁止：
未授权攻击、主动反制、上传密钥、GPL 污染、虚假“完整兼容”。

待办（我电脑上）：
在我的 Win 电脑“此电脑”的 D 驱动器里创建一个文件夹，用于创建 AI 工作集群（本地运行，无需 llama qwen 0.5B 多集群，创建出一个应用，最后不上传，真实模型）。

6. 上传与配置：
(1) 排除 dist/、build/、*.bin、*.elf、密钥、日志、SDK。
(2) 最后上传，但 readme.md 需标明出处。

先继续上下文任务（确保所有功能裸机可用不崩溃）。一个一个搞，不许跨度搞，都是上下文，每个任务挨个做。
